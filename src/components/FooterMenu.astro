---
// Single footer link column
import { url } from '@src/utils/path';
import type { FooterMenuData } from '@src/data/footerData';

interface Props extends FooterMenuData {}

const { title, links } = Astro.props;
---

<div>
  <p class="font-heading font-bold text-sm text-navy mb-4">{title}</p>
  <ul class="flex flex-col gap-3">
    {links.map((link) => (
      <li><a href={url(link.href)} class="text-muted text-sm hover:text-brand-600 transition-colors">{link.label}</a></li>
    ))}
  </ul>
</div>
