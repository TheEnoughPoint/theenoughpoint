# Dependencies
node_modules/

# Build output
dist/

# Astro generated types and cache
.astro/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.*

# Claude Code: share the project's skills and launch config, keep local state out.
# Skills are project tooling — /preflight is referenced by CLAUDE.md, so it has
# to travel with the repo rather than living on one machine.
.claude/settings.local.json
.claude/worktrees/

# The render audit is copied here so the dev server can serve it to the page
# under test (see .claude/skills/preflight). It must never ship to production.
public/render-audit.js

# Raw article-image inbox: source uploads and the tracking sheet, not deploy assets
public/img/Archive/
public/img/Images to Update.xlsx
